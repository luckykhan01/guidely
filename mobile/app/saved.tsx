import { View, Text } from 'react-native';

export default function Saved() {
  return (
    <View className="flex-1 bg-background items-center justify-center">
      <Text className="text-text text-xl font-bold">Saved Places</Text>
    </View>
  );
}
